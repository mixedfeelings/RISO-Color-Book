import { ColorModel } from './types';
interface Converter {
    fromComponents: (components: number[]) => number[];
    toComponents: (values: number[]) => number[];
}
export declare const conversion: Record<ColorModel, Converter>;
export {};
